apiVersion: rabbitmq.com/v1beta1
kind: Permission
metadata:
  name: dai-xld-admin-permissions
spec:
  vhost: "/"
  user: guest
  permissions:
    read: ".*"
    configure: ".*"
    write: ".*"
  rabbitmqClusterReference:
    connectionSecret:
      name: dai-xld-rabbitmq-secret

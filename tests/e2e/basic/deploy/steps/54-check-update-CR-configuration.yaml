apiVersion: kuttl.dev/v1beta1
kind: TestStep
unitTest: false
commands:
- command: kubectl exec  -n digitalai -c deploy -it dai-xld-digitalai-deploy-cc-server-0 -- cat centralConfiguration/type-defaults.properties | grep aws.Cloud.allowConcurrentDeployments=false
  namespaced: true
  timeout: 10